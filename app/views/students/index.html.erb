<% if @classroom %>
  <h2>Student Roster</h2>
  <h4><%= "Period #{@classroom.period} #{@classroom.subject} with #{@classroom.teacher}" %></h4>
  <br><%= link_to "See Only Students With Allergies", classroom_students_with_allergy_path(@classroom) %><br><br>
<% else %>
  <h2>All Students</h2>
  <br><%= link_to "See Only Students With Allergies", students_with_allergy_path %><br><br>
<% end %>

<table>
  <tr>
    <th>Last Name </th>
    <th>First Name </th> 
    <th>Grade </th>
    <th>Lunch </th>
  </tr>
  <% @students.each do |s| %>
    <tr>
      <td><%= s.last_name %></td> 
      <td><%= s.first_name %></td>
      <td><%= s.grade %></td> 
      <td><%= s.lunch_period %></td>
    </tr>
  <% end %>
</table>

<% if !@classroom %>
  <br><br><b>Add Multiple Students At Once?</b>
  <%= form_tag '/students/new', method: 'get' do %>
    <%= label_tag "How many Students?" %>
    <%= number_field_tag 'student_count', 2 , min: 2, max: 20, step: 1 %><br>
    <%= submit_tag "Create New Batch" %><br><br>
  <% end %>
<% end %>

